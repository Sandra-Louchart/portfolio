{% extends 'base.html.twig' %}

{% block title %}Administrative{% endblock %}

{% block body %}
    <h1>Administrative</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ administrative.id }}</td>
            </tr>
            <tr>
                <th>First</th>
                <td>{{ administrative.first }}</td>
            </tr>
            <tr>
                <th>Last</th>
                <td>{{ administrative.last }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ administrative.adress }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ administrative.number }}</td>
            </tr>
            <tr>
                <th>Zip</th>
                <td>{{ administrative.zip }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ administrative.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('administrative_index') }}">back to list</a>

    <a href="{{ path('administrative_edit', {'id': administrative.id}) }}">edit</a>

    {{ include('/admin/administrative/_delete_form.html.twig') }}
{% endblock %}
